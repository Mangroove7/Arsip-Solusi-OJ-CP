#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t; cin >> t;
    while(t--){
        int n,m,x,y;cin >> n >> m >> x >> y;
        int n_new = n, m_new = m;
        while(n_new--){
            int a; cin >> a;
        }
        while(m_new--){
            int b; cin >> b;
        }
        cout << n + m << endl;
    }
    return 0;
}