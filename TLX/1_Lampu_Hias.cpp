#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int cek(){
    int cnt = 0;
        int hai[s.size()];
        for(int i = 0;i < s.size();i++){
           if(t.find(s[i]) != -1){
            hai = t.find(s[i];
           }
           else return false;
        
        }
       int temp =  *sort(hai,sizeof(hai));

        return(tek == s);
}

int main(){
    
}