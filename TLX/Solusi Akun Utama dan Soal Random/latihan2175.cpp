#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int bilangan;
    scanf("%d",&bilangan);
    (bilangan >= 0)?printf("Bilangan Bulat Positif\n"):printf("\n");

    return 0;
}