#include <bits/stdc++.h>
using namespace std;
int pembanding(string seleksi,int nilaia,int nilaib,int nilaic){

}
int main(){
    ios_base::sync_with_stdio(false);cin.tie(0);
    int T;cin >> T;
    for(int i = 0;i < T;i++){
        int n,m;string dipilih;
        vector <int> listnilai(3);
        cin >> n >> m;
        string seleksi[n];
        cin >> dipilih;
        for(int j = 0;j < n;j++){
            int nilai[3];
            cin >> seleksi[j] >> nilai[0] >> nilai[1] >> nilai[2];
        }
        
    }
}